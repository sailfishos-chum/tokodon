diff --git a/CMakeLists.txt b/CMakeLists.txt
index 4704df17..6cebd332 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -102,7 +102,7 @@ else()
     )
 endif()
 
-if (UNIX AND NOT APPLE AND NOT ANDROID)
+if (UNIX AND NOT APPLE AND NOT ANDROID AND NOT SAILFISHOS)
     find_package(KF6KIO ${KF_MIN_VERSION})
     set_package_properties(KF6KIO PROPERTIES
             TYPE REQUIRED
@@ -110,7 +110,7 @@ if (UNIX AND NOT APPLE AND NOT ANDROID)
     )
 endif ()
 
-if (UNIX AND NOT APPLE AND NOT ANDROID AND NOT TOKODON_FLATPAK)
+if (UNIX AND NOT APPLE AND NOT ANDROID AND NOT TOKODON_FLATPAK AND NOT SAILFISHOS)
     find_package(KF6Purpose ${KF_MIN_VERSION})
     set_package_properties(KF6Purpose PROPERTIES
             TYPE REQUIRED
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 134ec467..7a0211e3 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -368,12 +368,14 @@ if (BUILD_TESTING)
     )
 endif()
 
-if(ANDROID)
-    target_sources(tokodon_static
-        PRIVATE
-        utils/androidutils.cpp
-        utils/androidutils.h
-    )
+if(ANDROID OR SAILFISHOS)
+    if(ANDROID)
+        target_sources(tokodon_static
+            PRIVATE
+            utils/androidutils.cpp
+            utils/androidutils.h
+        )
+    endif()
     qt_target_qml_sources(tokodon_static
         QML_FILES
         content/ui/Android/ShareAction.qml
@@ -636,6 +638,6 @@ if(BUILD_TESTING AND NOT ANDRIOID)
     install(TARGETS tokodon-offline ${KF6_INSTALL_TARGETS_DEFAULT_ARGS})
 endif()
 
-if (UNIX AND NOT APPLE AND NOT ANDROID AND NOT TOKODON_FLATPAK)
+if (UNIX AND NOT APPLE AND NOT ANDROID AND NOT TOKODON_FLATPAK AND NOT SAILFISHOS)
     add_subdirectory(purpose)
 endif ()
